# Claude will only scan these paths first (fast), still allowing manual loads elsewhere.
include:
  # Core build work
  - categories/01-core-development
  - categories/03-infrastructure
  - categories/06-developer-experience
  - categories/09-meta-orchestration
  - categories/05-data-ai

# (Optional) explicitly de-prioritize rarely used sets.
# Claude can still access these when you call an agent by name.
exclude: